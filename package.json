{
  "name": "youtube-companion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tooling manifest for the YouTube Companion extension. Chrome ignores this file; it exists so `npm test` works.",
  "scripts": {
    "test": "node test/run-all.js --summary",
    "test:verbose": "node test/run-all.js",
    "test:json": "node test/run-all.js --json",
    "check": "node test/check-syntax.js"
  }
}
